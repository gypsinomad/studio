rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * GLOBAL UNRESTRICTED ACCESS
     * Permitting all reads and writes to resolve persistent permission errors.
     */
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}