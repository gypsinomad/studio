
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "properties": {
        "authUid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "role": { "type": "string", "enum": ["admin", "salesExecutive", "viewer"] },
        "isActive": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "Lead": {
      "title": "Lead",
      "type": "object",
      "properties": {
        "fullName": { "type": "string" },
        "companyName": { "type": "string" },
        "email": { "type": "string" },
        "phone": { "type": "string" },
        "status": { "type": "string" },
        "source": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "ExportOrder": {
      "title": "ExportOrder",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "stage": { "type": "string" },
        "totalValue": { "type": "number" },
        "destinationCountry": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "AIUsageStats": {
      "title": "AIUsageStats",
      "type": "object",
      "properties": {
        "monthKey": { "type": "string" },
        "totalCallsMonth": { "type": "number" },
        "estimatedSpendThisMonthInr": { "type": "number" },
        "dailyCalls": { "type": "object" },
        "lastUpdatedAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["password", "google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/backend/entities/User" },
      "description": "User profiles"
    },
    "/leads/{leadId}": {
      "schema": { "$ref": "#/backend/entities/Lead" },
      "description": "Sales leads"
    },
    "/exportOrders/{orderId}": {
      "schema": { "$ref": "#/backend/entities/ExportOrder" },
      "description": "Export orders"
    },
    "/activity_logs/{logId}": {
      "schema": { "type": "object" },
      "description": "Activity audit logs"
    },
    "/tasks/{taskId}": {
      "schema": { "type": "object" },
      "description": "Tasks"
    },
    "/settings/ai": {
      "schema": { "type": "object" },
      "description": "AI configuration"
    },
    "/usageStats/{monthId}": {
      "schema": { "$ref": "#/backend/entities/AIUsageStats" },
      "description": "Monthly AI usage tracking"
    },
    "/whatsappEvents/{eventId}": {
      "schema": { "type": "object" },
      "description": "Incoming WhatsApp webhook events"
    }
  }
}
